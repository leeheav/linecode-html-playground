# 部署指南

复制 `.env.example` 为 `.env`，并配置：

- `ADMIN_USERNAME`：管理员用户名。
- `ADMIN_PASSWORD`：至少 12 个字符的强密码。
- `ADMIN_SESSION_SECRET`：生产环境必填，使用独立的至少 32 字节随机密钥。

条件允许时，管理员后台与用户生成的 HTML 应部署在不同的 origin（不同域名）；管理员 Cookie 不应设置父域 `Domain`。当前后台预览使用不带同源权限的沙箱 iframe，避免用户 HTML 读取后台会话。

## 站点存储与 7 天有效期

生成的 HTML 存放在 `data/sites`（也可用 `SITES_DIR` 指向持久化绝对路径），不再放在 `public`。每个 HTML 都有对应的 `.meta.json`，公开链接创建满 7×24 小时后返回 HTTP 410，但文件不会自动删除，管理员仍可在后台预览。

如果当前 `npm` 命令实际通过 Docker 运行，默认的项目内 `data/sites` 最省事；若设置 `SITES_DIR`，必须确保该容器路径映射到宿主持久化目录，否则重建容器后数据会丢失。

旧版本升级时，先停止应用，再在项目根目录执行一次：

```bash
npm run migrate:sites
```

迁移会把 `public/sites/*.html` 安全移动到新存储目录并生成到期元数据。创建时间优先沿用文件创建时间。

Nginx **必须删除**旧的静态目录配置，否则访问 `/sites/文件名.html` 会绕过应用的过期检查：

```nginx
# 删除整个配置块：
location /sites/ {
    root /home/html/linecode-html-playground/public;
    try_files $uri $uri/ =404;
}
```

可以额外加入防御性规则：

```nginx
location ^~ /sites/ {
    return 404;
}
```

所有正常请求继续由 `location /` 代理到 Next.js。

## 构建与启动

```bash
npm run build
npm start
```

生产环境应使用 systemd、PM2 或其他进程管理器保持服务运行；Nginx 只负责反向代理，不能代替 Node.js 进程。
