# 部署指南

复制 `.env.example` 为 `.env`，并配置：

- `ADMIN_USERNAME`：管理员用户名。
- `ADMIN_PASSWORD`：至少 12 个字符的强密码。
- `ADMIN_SESSION_SECRET`：生产环境必填，使用独立的至少 32 字节随机密钥。

管理员后台与用户生成的 HTML 应部署在不同的 origin（不同域名），且管理员 Cookie 不应设置父域 `Domain`。

使用pm2部署, 在项目根目录执行：
`npm run build`
`pm2 start npm --name "play" -- start`
